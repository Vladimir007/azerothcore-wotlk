find_package(BZip2 REQUIRED)
add_library(bzip2 SHARED IMPORTED GLOBAL)
set_target_properties(bzip2 PROPERTIES IMPORTED_LOCATION "${BZIP2_LIBRARIES}" INTERFACE_INCLUDE_DIRECTORIES "${BZIP2_INCLUDE_DIRS}")
