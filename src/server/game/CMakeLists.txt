CollectSourceFiles(${CMAKE_CURRENT_SOURCE_DIR} PRIVATE_SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/PrecompiledHeaders)
CollectIncludeDirectories(${CMAKE_CURRENT_SOURCE_DIR} PUBLIC_INCLUDES ${CMAKE_CURRENT_SOURCE_DIR}/PrecompiledHeaders)

add_library(game-interface INTERFACE)
target_include_directories(game-interface INTERFACE ${PUBLIC_INCLUDES})
target_link_libraries(game-interface INTERFACE shared)
add_library(game STATIC ${PRIVATE_SOURCES})
target_include_directories(game PRIVATE ${CMAKE_CURRENT_BINARY_DIR})
target_link_libraries(game PRIVATE ncore-core-interface PUBLIC game-interface)
set_target_properties(game PROPERTIES FOLDER "server")
